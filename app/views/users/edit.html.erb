<!-- This view is displayed when editing a user. -->
<div style="padding-top: 10px;">
<!-- Checks if the user is an admin, if the user is an admin, display the heading text slightly different so it makes sense. -->
<% if logged_in? && is_administrator? %>
  <h1>Update User Details</h1>
  <% else %>
  <h1>Update Your Details</h1>
  <% end %>
</div>
<hr />


<!-- Renders the form in views/users/_form with the current user selected. -->
<%= render "form", user: @user %>

<!-- Checks if the user is an admin, if the user is an admin, display the show user and back to users (user database) page. -->
<% if logged_in? && is_administrator? %>
<div style="padding-top: 10px;">
<div>
  <%= link_to "Show this user", @user %> |
  <%= link_to "Back to users", users_path %>
</div>
</div>
<% end %>